<section>
  {{#each restaurants}}
  {{!-- <article class="card">
  <img src="{{this.image}}" alt="{{this.name}}" style="width:100%">
  <div class="container">
    <h4><b>{{this.name}}</b></h4> 
  </div>
</article> --}}
  <a href="/restaurants/{{this.id}}">
    <article class="card" style="background-image: url({{this.image}});">
      <div class="title">
        <h3><strong>{{this.name}}</strong></h3>
      </div>
      <div class="menus">
        <small>Menus</small>
        {{#each this.menus}}
        <p>{{this.title}}</p>
        {{/each}}
      </div>
    </article>
  </a>
  {{/each}}
  <article class="addMenu">
    <form action="/restaurants" method="POST">
      <label for="name">Restaurant Name:</label>
      <input type="text" id="name" name='name' placeholder="name" required />
      <br>
      <label for="imgURL">Image URL:</label>
      <input type="url" id="imgURL" name='imgURL' placeholder="URL" required />
      <button type="submit">Add Restaurant</button>
    </form>
  </article>
</section>